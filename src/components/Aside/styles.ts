import styled from 'styled-components';

export const Container = styled.div`
    /** Após setar no styles.ts do layout as definições de tamanho, precisa definir em cada arquivo style para chamar aquelas configs */
    
    grid-area: AS;
    background-color: #252a48;
    color: white;

`;